<?php
$escape = static function ($value) {
    return htmlspecialchars((string)$value, ENT_QUOTES, 'UTF-8');
};

$confirmationPhrase = $escape($this->cleanupConfirmationPhrase);
?>

<details class="form-box nebulynk-card nebulynk-cleanup-card">
    <summary class="nebulynk-section-head nebulynk-cleanup-summary">
        <div>
            <p class="nebulynk-section-kicker">Danger zone</p>
            <h3>Remove Nebulynk completely</h3>
        </div>
        <span class="nebulynk-tag">Irreversible</span>
    </summary>

    <div class="nebulynk-cleanup-content">

        <div class="nebulynk-callout nebulynk-callout-error">
            <div class="nebulynk-callout-icon" aria-hidden="true">!</div>
            <div>
                <strong>All Nebulynk data will be deleted</strong>
                <p>
                    This action stops the stack and deletes the deployment at
                    <strong>/opt/nebulynk-plesk</strong>, including the source,
                    <strong>.env</strong>, PostgreSQL, Redis, and Garage data.
                    This deletion cannot be undone.
                </p>
            </div>
        </div>

        <p class="nebulynk-card-intro">
            Create and verify a backup first. The Plesk domain, DNS, TLS
            certificate, and firewall rules are not changed. Enter
            <code><?= $confirmationPhrase ?></code> exactly to confirm.
        </p>

        <?= $this->cleanupForm ?>

        <p class="nebulynk-form-note">
            If proxy or Docker cleanup fails, no files are deleted. Shared Docker
            images and the global Docker cache are retained.
        </p>
    </div>
</details>
